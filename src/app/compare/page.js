"use client";

import { useEffect, useMemo, useState } from "react";
export default function CompareRedirect() {
  return (
    <div style={{ padding: "2rem", textAlign: "center" }}>
      Esta página se movió a /ELO_Pareja
    </div>
  );
}
const COLUMN_INDEX = {
